---
scope: GrowBase full application — brownfield S0→S4 implemented
purpose: Build substrate for future feature development — fix invariants, prevent builder divergence
altitude: initiative → features
updated: 2026-06-27T13:19
---

- (constraint) Brownfield: S0→S4 fully implemented — Next.js 14 App Router, Supabase, TypeScript strict, Zustand, TanStack Query v5, shadcn/ui Spike Admin theme, ApexCharts, i18n vi/en, sonner
- (constraint) Household is the sole tenancy boundary — all domain data (transactions, funds, accounts, budget, income, debt) carries household_id. No sub-boundary below household.
- (decision) AD-1: Auth invariant. Every API route MUST call withAuth() as first line. Response shape MUST be { data: T | null, error: string | null } — both fields always present. Binds: all route authors. Prevents: silent auth bypass, inconsistent error handling. Rule: withAuth() first; never return { data } without error field or vice versa. [ADOPTED — bug in /api/household and /api/households violates this, must fix]
- (decision) AD-2: Supabase client model — Hybrid. User client (createServerClient + anon key, RLS enforced) for all user-facing data ops. Admin client (service role, RLS bypassed) only for system ops: onboarding household creation, invite token generation, fund RPC (contribute/withdraw/release). Binds: every route author must choose client based on op type. Prevents: blanket admin client use that removes DB-level security. Rule: user-scoped op → user client; system/cross-user op → admin client.
- (decision) AD-3: Household switching state contract. householdId persists in Zustand store (localStorage). On switch: householdId updates → currentMonth resets to current month → TanStack Query cache auto-invalidates (keys include householdId). Rule: any new store state that is household-scoped MUST clear on switch or be derived from keys including householdId.
- (decision) AD-4: Route protection model. All protected routes gated by middleware.ts (server-side, Edge runtime). Gates: !user → redirect /login; !onboarding_completed → redirect /setup. Layout (app)/layout.tsx renders AppShell only — no auth checks. API routes excluded from middleware — protected by withAuth() per-route. Rule: auth check belongs in middleware or withAuth(), never in layout.
- (decision) AD-5: Runtime model — self-hosted Node.js. API routes: Node.js runtime, never declare export const runtime = 'edge'. Middleware: Edge (Next.js mandatory) — uses only @supabase/ssr createServerClient, never supabaseAdmin. Rule: supabaseAdmin is forbidden in middleware.
- (decision) AD-6: Household membership double guard. Every API route touching household-scoped data: (1) withAuth() to identify user, (2) explicitly verify user is active member of householdId (query household_members), (3) execute operation. Applies even when RLS is on — double defense. Prevents: householdId spoofing.
- (constraint) [ADOPTED] Fund balance mutations: always atomic Supabase RPC. Never direct INSERT/UPDATE to fund balance. Rule 1 in CLAUDE.md.
- (constraint) [ADOPTED] Immutable fields: behavior_type = DB trigger only. is_system=true records = no edit/delete.
- (constraint) [ADOPTED] Query keys: always via keys.* factory. Keys include householdId. Never hardcode key arrays.
- (constraint) [ADOPTED] i18n: all UI strings via t(). No hardcoded text in components.
- (constraint) [ADOPTED] Component pattern: page.tsx = thin wrapper → <FeatureClient /> ('use client'). All logic in Client component.
- (question) Open: households table RLS status — middleware queries it with anon key (user client). Verify before RLS migration of other tables.
- (direction) Deferred: Write RLS policies for transactions, funds, accounts, budget, categories, income_sources, debt_entries. Pattern: auth.uid() IN (SELECT user_id FROM household_members WHERE household_id = table.household_id AND is_active = true). Revisit when hybrid migration begins.
- (event) Coaching complete — 6 ADs elicited, 5 adopted constraints ratified, 1 open question, 1 deferred. Distilling spine.
- (event) spine finalized — lint passed 0 findings, status set to final
