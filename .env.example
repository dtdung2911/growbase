NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Chỉ set khi deploy sau reverse proxy (prod): app tự quyết origin cho redirect OAuth + link mời.
# Bỏ trống ở dev/local (dùng origin của request). Build-time → rebuild sau khi đổi. Không để rỗng.
# NEXT_PUBLIC_SITE_URL=https://your-app-domain.com
