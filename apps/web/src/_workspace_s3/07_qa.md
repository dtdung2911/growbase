SPRINT:S3 AGENT:qa VERDICT:PASS
CROSS_BOUNDARY:
  GET /api/dashboard ↔ useDashboardData ↔ DashboardClient:OK:response={data:DashboardData,error}→hook reads json.data→component destructures totalIncome|totalExpense|savingsRate|spendingByBehavior|budgetLines|funds|recentTransactions. All fields match DashboardData type. Error path reads json.error.
  GET /api/budget ↔ useBudgetActuals ↔ BudgetClient:OK:route calls rpc get_budget_with_actuals→returns BudgetActualLine[]→hook reads json.data→BudgetClient maps cost_type_id|cost_type_name|budget_amount|actual_amount|usage_pct. All match.
  POST /api/budget/override ↔ useBudgetOverrideMutation ↔ BudgetOverrideInput:OK:hook sends {budget_baseline_id,month,override_pct}→route validates via budgetOverrideSchema(same 3 fields)→upsert includes household_id from auth(C1 FIXED). Component sends costTypeId as budget_baseline_id + firstDayOfMonth(month) + override_pct.
  DELETE /api/budget/override ↔ useDeleteBudgetOverride:OK:hook sends {budget_baseline_id,month}→route validates via budgetOverrideDeleteSchema→delete includes household_id(C2 FIXED).
  GET /api/scheduled-payments ↔ useScheduledPayments ↔ ScheduledPaymentsClient:OK:route returns data[]→hook enriches with days_until_due+urgency_level→ScheduledPayment type matches. Component filters active.
  POST /api/scheduled-payments ↔ useCreateScheduledPayment:OK:hook sends ScheduledPaymentCreateInput{name,period,amount,payment_method?,next_due_date,notes?}→route validates same schema→insert adds household_id.
  PUT /api/scheduled-payments/[id] ↔ useUpdateScheduledPayment:OK:hook sends {id,...partial+status?}→route uses partial schema+status(M5 FIXED via Zod).
  POST /api/scheduled-payments/[id]/mark-paid ↔ useMarkPaymentPaid ↔ MarkPaidDialog:OK:dialog sends {paymentId,create_transaction,account_id?,category_id?,date}→hook extracts paymentId→route body→markPaidFormSchema→rpc params(p_payment_id,p_household_id,p_create_transaction,p_account_id,p_category_id,p_member_id,p_date). All match. RPC returns uuid→route wraps as {payment_id,tx_id}→hook reads as {payment_id,tx_id|null}.
  GET /api/net-worth ↔ useNetWorthSnapshot ↔ NetWorthClient:OK:route returns {snapshot,systemBalances[],funds[]}→hook type NetWorthPageData matches→component reads systemBalances as SystemBalance[]+snapshot.items+funds.
  POST /api/net-worth ↔ useUpsertNetWorth:OK:hook sends NetWorthSnapshotInput{snapshot_month,items[],total_recorded,total_system,notes?}→route validates same schema→upsert adds household_id.
  GET /api/net-worth/history ↔ useNetWorthHistory ↔ NetWorthChart:OK:route returns {snapshot_month,total_recorded,total_system,discrepancy}[]→hook type NetWorthHistoryItem matches→chart maps to {month,recorded,system}.
  RPC get_system_balances→TABLE(account_id,account_name,account_type,system_balance):OK:matches SystemBalance type exactly(4 fields).
  RPC mark_payment_paid→uuid:OK:7 params match API route call. SECURITY_DEFINER+auth guard+FOR UPDATE.
BR_CHECK:
  R1-fund-ops:OK:no fund ops in S3 scope. mark_payment_paid creates expense tx only(not fund op)
  R2-behavior_type:OK:not in any form schema or mutation payload. Dashboard reads from DB only(tx.behavior_type)
  R3-is_system:OK:not relevant to S3 tables(no is_system on S3 entities)
  R4-exclude_from_budget:OK:dashboard filters tx.exclude_from_budget_report correctly(line 62 dashboard route)
  R5-auth:OK:ALL 9 route handlers call withAuth() first→return auth.error if no session
  R6-keys-factory:OK:ALL hooks use keys.* from queryKeys.ts. No hardcoded strings. 7 new key fns added.
  R7-touch-targets:OK:BudgetOverrideInput buttons h-[44px] w-[44px]. MarkPaidDialog buttons min-h-[44px]. DesktopDrawer links min-h-[44px]. ScheduledPaymentsClient Button min-h-[44px]. Input text-base(16px) confirmed.
  BR-BU-001:OK:get_budget_with_actuals handles overrides via LEFT JOIN(existing RPC)
  BR-BU-002:OK:budget_override upsert ON_CONFLICT budget_baseline_id+month
  BR-NW-001:OK:net_worth_snapshots UNIQUE(household_id,snapshot_month)→upsert uses onConflict
  BR-NW-002:OK:discrepancy=GENERATED ALWAYS→not in upsert payload→DB computes
  BR-SP-001:OK:DueBadge urgency thresholds: overdue(<0)|due-soon(<=7)|upcoming(<=30)|normal(>30). Red for <=7d, orange for <=30d.
  BR-SP-002:OK:mark_payment_paid advances next_due_date: monthly+1mo|quarterly+3mo|yearly+1yr
  BR-SP-003:OK:mark_payment_paid optionally creates tx when p_create_transaction=true
FIXES_VERIFIED:
  C1:OK:budget override upsert line 24 has household_id:auth.householdId
  C2:OK:budget override DELETE line 61 has household_id:auth.householdId in .match()
  C3:OK:BudgetOverrideRow line 189 has household_id:string
  C4:OK:BudgetOverrideInput line 62 has text-base only(no text-sm conflict)
  W1:OK:NetWorthAccountRow input has text-base only(confirmed no text-sm)
  W2:OK:DueBadge uses t("scheduledPayments.overdueDays")|t("scheduledPayments.today")|t("scheduledPayments.daysLeft")
  W3:OK:SpendingTab uses t() keys(reports.ofExpense|ofIncome|totalExpense)
  W4:OK:FundReportTab uses t("reports.totalFunds")
  W5:OK:IncomeTab uses t("reports.regularIncome")|t("reports.unusualIncome")
  W6:OK:MarkPaidDialog uses t("scheduledPayments.account")|selectAccount|category|selectCategory
  W7:OK:NetWorthChart uses t("common.monthLabel",{month:label})
STYLE_CHECK:
  semantic-tokens:OK:bg-card|bg-surface|bg-elevated|bg-inset|text-foreground|text-muted-foreground used throughout. No hardcoded colors except brand copper(#D97757) in chart.
  font-mono:OK:all amount displays use font-mono tabular-nums
  AppShell-responsive:OK:lg:pl-60 for desktop+max-w-md mobile+max-w-5xl desktop. CSS breakpoints(hidden lg:flex|lg:hidden). No useMediaQuery.
  ThemeProvider:OK:attribute="class"|defaultTheme="light"|storageKey="growbase-theme" in providers.tsx
  TranslationProvider:OK:wraps app in providers.tsx. vi.json+en.json have all 91 keys each.
  pb-16:OK:AppShell main has pb-16 lg:pb-0
TSC:PASS(0 errors)
VITEST:PASS(217/217)
WARNINGS:
  W-QA-1:NavBadge due_soon_count not wired to BottomNav(deferred per developer log). Minor UX gap.
  W-QA-2:dashboard route uses 3 parallel queries not 4(arch D1 said 4). tx+budget+funds. Income computed from tx. Functionally correct, cosmetic deviation.
  W-QA-3:DueBadge uses red for both overdue AND due-soon(<=7d). Arch spec says red_<=7d|orange_<=30d. Currently: overdue=red|due-soon=red|upcoming=orange. Overdue being red correct. Due-soon(1-7d) should arguably differ from overdue visually but acceptable for MVP.
  W-QA-4:migration 008 not tested against live DB(Docker down per 03_mig.md). SQL reviewed, patterns match existing RPCs.
  W-QA-5:S1/S2 pages not retrofitted with i18n(per plan, deferred to S4)
KNOWN:
  K1:database.ts manual types→supabase gen types(S2 carry)
  K2:type cast GET /api/transactions→typed DB interface(S2 carry)
  K3:internal_transfer not atomic→DB function(S2 carry)
  K4:system category lookup by name→code field(S2 carry)
  K5:NavBadge wiring→S4
SHIP:YES
