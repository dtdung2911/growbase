SPRINT:S2 AGENT:qa VERDICT:PASS
CROSS_BOUNDARY:
  POST /api/transactions:OK:createTransactionSchema fields=amount+direction+transaction_type+category_id+account_id+description+transaction_date+is_unusual_income+debt_entry_id→hook payload=CreateTransactionInput(same)→API insert matches→response {id}→hook reads json.data as {id:string}
  PUT /api/transactions/[id]:OK:updateTransactionSchema=create+id→hook sends {...data,id}→API parses id from params+body→system tx guard C1 FIXED→response {id}
  DELETE /api/transactions/[id]:OK:hook sends id string→API fetches existing+system guard→response {id}→ConfirmDialog wraps delete
  POST /api/transactions/transfer:OK:createTransferSchema fields=from_account_id+to_account_id+amount+description+transaction_date+is_credit_card_payment→hook payload=CreateTransferInput(same)→API creates 2 INSERTs→response {out_id,in_id}→hook reads as {out_id:string,in_id:string}
  POST /api/funds/[id]/contribute:OK:fundContributeSchema=amount+account_id+description+transaction_date→hook payload=FundContributeInput(same)→API uuid validates params.id→lookups system category→calls RPC fund_contribute(8 args match spec)→response {tx_id}
  POST /api/funds/[id]/withdraw:OK:fundWithdrawSchema=amount+account_id+category_id+description+transaction_date→hook payload=FundWithdrawInput(same)→API uuid validates→balance check R8→calls RPC fund_withdraw(8 args match spec)→response {tx_id}
  POST /api/funds/[id]/release:OK:hook sends no body→API uuid validates→UPDATE funds.released_at .maybeSingle()→response {released_at}→hook reads {released_at:string}
  GET /api/transactions:OK:hook queryKey=keys.transactions(hid,month)→fetch /api/transactions?month=M→API returns {data:rows[]+category+account joins}→hook returns TransactionWithJoins[]→TransactionList renders
  GET /api/funds:OK:hook queryKey=keys.funds(hid)→fetch /api/funds→API returns {data:Fund[]}→FundList+FundCard render
  GET /api/accounts:OK:hook queryKey=keys.accounts(hid)→fetch /api/accounts→API returns {data:Account[]}→used in form pickers
  ERROR_FORMAT:OK:all routes return {data:null,error:string}→all hooks throw new Error(json.error)→onError uses err.message→toast.error(msg,{duration:5000})
  INVALIDATION:OK:create/update tx→transactions+budget+accounts|delete tx→transactions+budget|transfer→transactions+accounts+budget|fund contribute/withdraw→funds+transactions+fundTransactions|fund release→funds only
BR_CHECK:
  BR-TX-001:OK:createTransactionSchema requires amount>0+direction+category_id+account_id+transaction_date
  BR-TX-002:OK:behavior_type not in any schema/mutation payload, readonly Badge in TransactionForm, set by DB trigger (003+007)
  BR-TX-003:OK:is_unusual_income toggle shown only when direction="in", default false
  BR-TX-004:OK:transfer creates 2 rows type=internal_transfer, DB trigger sets exclude_from_budget_report=true
  BR-TX-005:OK:debt_repayment type shows debt_entry_id dropdown, optional field in schema
  BR-FU-001:OK:fund_contribute+fund_withdraw use .rpc() calls, NOT sequential queries
  BR-FU-002:OK:balance check both client-side (WithdrawForm exceedsBalance) + server-side (withdraw route line 45)
  BR-CO-001:OK:CategoryPicker filters by cost_type_code (not fragile string matching)
  BR-CO-002:OK:category_groups query includes .or(household_id.eq.hid,household_id.is.null) for system+custom categories
  BR-CO-003:OK:no behavior_type in form fields or mutations
  BR-CO-004:OK:contribute uses system category "Tiet kiem" looked up by is_system=true+name
  BR-CA-001:OK:accounts API filters is_active=true, ordered by sort_order
  BR-CA-002:OK:accounts used in all forms (TransactionForm, InternalTransferForm, ContributeModal, WithdrawForm)
  BR-SY-001:OK:system tx guard in PUT/DELETE (line 38-44, 89-96 of [id]/route.ts) + TransactionEditSheet shows read-only for system types
  BR-BU-002:OK:MonthlyBufferBanner checks fund_type=monthly_buffer+balance>0+day<=10+released_at not current month
NON_NEGOTIABLE:
  R1:OK:contribute route line 47 .rpc("fund_contribute"), withdraw route line 53 .rpc("fund_withdraw"). No .from("transactions") in either
  R2:OK:behavior_type only appears as readonly Badge display in TransactionForm:80,254. Not in createTransactionSchema, updateTransactionSchema, or any mutation payload
  R3:OK:SYSTEM_TYPES guard in PUT (line 38-44) and DELETE (line 89-96) of transactions/[id]/route.ts returns 403. TransactionEditSheet line 39-40 shows read-only view
  R5:OK:ALL 12 API routes use withAuth() as first call. 4 pre-existing routes (household, invite, onboarding) use own auth patterns (not S2 scope)
  R6:OK:grep found 0 hardcoded queryKey strings in hooks. All use keys.* factory from queryKeys.ts
  R7:OK:BottomNav min-h-[44px], TransactionItem min-h-[44px], buttons use size="sm" (min-h-[44px] in button.tsx)
  R8:OK:WithdrawForm client check (line 50 amount>current_balance) + withdraw API server check (line 45 input.amount>fund.current_balance→400)
CRITICAL_FIXES_VERIFIED:
  C1:FIXED:system tx guard in PUT/DELETE→checked actual code lines 38-44+89-96
  C2:FIXED:useCategories .or(household_id.eq.hid,household_id.is.null)→checked line 48
  C3:FIXED:z.string().uuid() on params.id in contribute(line12)+withdraw(line12)+release(line11)+transactions(line11)
  C4:FIXED:.maybeSingle() in release route→checked line 26
WARNINGS:
  W1=transfer atomicity→2 INSERTs with cleanup on partial fail+console.error on cleanup fail→ACCEPTABLE for S2, DB fn deferred to S3
  W4=category name lookup for system categories→still uses name string "Tiet kiem"+"Chuyen khoan noi bo"→fragile but no better option without DB schema change→KNOWN
KNOWN:
  K1=database.ts manual types→replace with supabase gen types S3
  K2=M1 type cast in GET /api/transactions→typed DB interface S3
  K3=transfer not truly atomic→DB function S3
BUILD:TSC=0_errors VITEST=161/161_pass
SHIP:YES
