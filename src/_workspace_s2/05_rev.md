SPRINT:S2 AGENT:reviewer STATUS:NEEDS_FIX
CRITICAL:4 WARNING:6 MINOR:5
ISSUES:
  C1:src/app/api/transactions/[id]/route.ts:22-35:R3 violation→PUT/DELETE allows editing system-generated transactions (fund_contribution, fund_withdrawal, internal_transfer). No check for transaction_type or behavior_type before allowing mutation→Add guard: fetch existing tx, if transaction_type in [internal_transfer,fund_contribution,fund_withdrawal] return 403 "Giao dich he thong khong the sua/xoa"
  C2:src/lib/hooks/useCategories.ts:41-47:SECURITY→useCategories queries category_groups without filtering by household_id. Returns ALL categories across all households via direct Supabase client call (bypasses RLS if misconfigured)→Add .or("household_id.eq.<hid>,household_id.is.null") filter using the passed householdId param
  C3:src/app/api/funds/[id]/contribute/route.ts:7,src/app/api/funds/[id]/withdraw/route.ts:7,src/app/api/funds/[id]/release/route.ts:6,src/app/api/funds/[id]/transactions/route.ts:6:SECURITY→params.id used directly in RPC/queries without UUID format validation. Malicious non-UUID string passed to Supabase→Add z.string().uuid() validation on params.id at top of each handler, return 400 if invalid
  C4:src/app/api/funds/[id]/release/route.ts:20-24:LOGIC BUG→.single() throws error if no row matches (fund not found or not monthly_buffer), but the !data check on line 23 is unreachable because .single() already threw→Change .single() to .maybeSingle(), then the null check works correctly
WARNINGS:
  W1:src/app/api/transactions/transfer/route.ts:48-97:K2 ATOMICITY→Transfer creates 2 separate INSERTs. If outTx succeeds but inTx fails, cleanup DELETE on line 87-90 can also fail silently (no error check on delete result)→Log or surface cleanup failure. Consider wrapping in DB function for true atomicity in S3
  W2:src/lib/hooks/useTransfer.ts:27:MISSING INVALIDATION→Transfer only invalidates transactions, misses accounts and budget queries. Account balances shown in UI will be stale after transfer→Add invalidation for keys.accounts(householdId) and keys.budget(householdId, month)
  W3:src/lib/hooks/useTransactions.ts:43-51:MISSING INVALIDATION→useCreateTransaction and useUpdateTransaction do not invalidate accounts query. If account balance is derived/cached, UI shows stale data→Add keys.accounts(householdId) invalidation in onSuccess
  W4:src/components/transactions/CategoryPicker.tsx:29:K3 FRAGILE→INCOME_GROUPS=["Thu nhap"] relies on exact Vietnamese group name from seed data. If seed changes group name, income/expense filter breaks silently→Use cost_type_id or a DB column (e.g., cost_type.code="income") instead of string matching
  W5:src/components/transactions/TransactionEditSheet.tsx:37-53:R3 INCOMPLETE→No check if transaction is system-generated before showing edit form. User can open edit sheet for fund/transfer transactions→Check transaction.transaction_type; if system type, show read-only view or hide edit/delete actions
  W6:src/components/layout/BottomNav.tsx:35:R7 TOUCH TARGET→Nav items use py-2 (8px top+bottom = ~36px height) which is below 44px minimum. text-[10px] label makes total tap area too small→Increase to py-3 or min-h-[44px] on each nav link
MINOR:
  M1:src/app/api/transactions/route.ts:36:TYPE SAFETY→`row as Record<string, unknown>` casts away type information for the join reshape. Could use a proper typed interface instead
  M2:src/components/transactions/TransactionItem.tsx:26-29:TOUCH TARGET→TransactionItem button uses py-2.5 (10px vertical). Total height depends on content but may fall below 44px for single-line items→Consider min-h-[44px]
  M3:src/types/database.ts:1-2:TECH DEBT→Manual database types placeholder acknowledged in K4. Plan to replace with supabase gen types in S3
  M4:src/components/funds/FundCard.tsx:89-104:R7→Fund action buttons use size="sm" which maps to min-h-[44px] (OK per button.tsx). Verified compliant
  M5:src/components/onboarding/WizardStep5Debt.tsx:124:DARK MODE→text-zinc-400 is hardcoded color, should use text-muted-foreground for theme consistency
APPROVED_IF_ZERO_C:false
GOOD:R1 fund_contribute+fund_withdraw use RPC correctly|R2 behavior_type readonly in UI with badge display|R5 withAuth helper DRYs auth+household check across all 12 routes|R6 all query keys use keys.* factory|R7 buttons+selects+inputs all min-h-[44px] text-base|R8 fund balance check both client(WithdrawForm)+server(API)|toast durations correct 2s success 5s error|ConfirmDialog used for destructive actions|skeleton loading pattern for lists|Zod validation on all POST/PUT bodies|household ownership verified on all data access
